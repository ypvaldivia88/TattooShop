{% extends 'base.html.twig' %}

{% block body %}
    <h1>Artista</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artistum.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ artistum.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('artista_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('artista_edit', { 'id': artistum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
