{% extends 'base.html.twig' %}

{% block body %}
    <h1>TipoTrabajo list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tipoTrabajo in tipoTrabajos %}
            <tr>
                <td><a href="{{ path('tipotrabajo_show', { 'id': tipoTrabajo.id }) }}">{{ tipoTrabajo.id }}</a></td>
                <td>{{ tipoTrabajo.nombre }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('tipotrabajo_show', { 'id': tipoTrabajo.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('tipotrabajo_edit', { 'id': tipoTrabajo.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipotrabajo_new') }}">Create a new entry</a>
        </li>
    </ul>
{% endblock %}
