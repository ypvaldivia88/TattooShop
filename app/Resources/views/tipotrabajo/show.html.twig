{% extends 'base.html.twig' %}

{% block body %}
    <h1>TipoTrabajo</h1>

    <table id="DT_Usuarios" class="table table-bordered table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipoTrabajo.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ tipoTrabajo.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('tipotrabajo_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('tipotrabajo_edit', { 'id': tipoTrabajo.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
